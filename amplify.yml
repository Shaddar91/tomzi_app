version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - mkdir -p build
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*